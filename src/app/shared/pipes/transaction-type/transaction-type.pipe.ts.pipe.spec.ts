import { TransactionTypePipeTsPipe } from './transaction-type.pipe.ts.pipe';

describe('TransactionTypePipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionTypePipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
